# Распределение задач — Проект 1, Вариант 2

**Группа:** М3О-103БВ-25
**Тема:** Калькулятор на C++/Qt6 с поддержкой `ln`, `exp`, `sqrt`, `abs`
**Тимлид / интегратор:** Гаценко Александр Олегович

> Стек уже выбран: **C++17 + Qt6 + CMake**. Вся базовая логика (парсер, вычислитель, GUI, тесты) написана и закоммичена в `src/` и `tests/`. Тесты проходят (41/41). Дальше команде нужно: собрать на своих машинах, протестировать вручную с GUI, сделать скриншоты и оформить отчёт.

---

## Гаценко Александр Олегович — Тимлид / Интеграция

**Зона ответственности:** сборка проекта, координация, code review, финальная сборка отчёта.

- [ ] Развернуть проект локально (`brew install qt cmake` → `cmake -B build -S . && cmake --build build -j`).
- [ ] Запустить юнит-тесты: `cd build && ctest --output-on-failure`. Должно быть 41/41.
- [ ] Запустить GUI: `open ./build/calculator.app`. Убедиться что окно открывается, кнопки работают.
- [ ] Собрать у каждого члена команды его часть, помочь если что-то не собирается.
- [ ] Сделать ревью разделов отчёта от других участников (один день перед сдачей).
- [ ] Оформить титульный лист отчёта (раздел «Титульный лист» в `report.md`).
- [ ] Создать общий PDF / Word из `report.md` для сдачи преподавателю (Полтавский Д. С., каб. 440).

**В отчёт пишет:** Титульный лист, Заключение.

---

## Загорный Тимофей Максимович — Парсер выражений

**Зона ответственности:** разобраться в `src/parser.{h,cpp}` и описать его в отчёте.

- [ ] Прочитать `src/parser.h` и `src/parser.cpp`.
- [ ] Понять и уметь объяснить:
  - что такое токенизация (метод `tokenize`),
  - алгоритм Shunting-yard Дейкстры (метод `to_rpn`),
  - как обрабатывается унарный минус (`u-`),
  - таблица приоритетов операторов (метод `precedence`),
  - как определяются функции (`is_function`).
- [ ] Прогнать тесты вручную, посмотреть какие выражения парсятся в RPN (можно добавить отладочный вывод).
- [ ] Написать раздел «Парсер» в отчёте: описание токенов, шагов алгоритма, пример трассировки `2+3*4` → `2 3 4 * +`.
- [ ] Нарисовать блок-схему функции `tokenize` (основной цикл с ветвлениями по типу символа).

**В отчёт пишет:** Раздел «Разработка → Парсер выражений» + блок-схема `tokenize`.

---

## Калинин Кирилл Евгеньевич — Вычислитель и обработка ошибок

**Зона ответственности:** `src/evaluator.{h,cpp}` + раздел про ошибки.

- [ ] Прочитать `src/evaluator.h` и `src/evaluator.cpp`.
- [ ] Понять:
  - как стек используется при вычислении RPN,
  - все 4 функции варианта (`ln`, `exp`, `sqrt`, `abs`) и где они проверяются на корректность,
  - какие ошибки выбрасываются (см. блок `catch` в `MainWindow::compute`).
- [ ] Составить таблицу всех обрабатываемых ошибок (см. готовый список в `README.md` + добавить из кода).
- [ ] Написать раздел «Вычислитель» в отчёте: описание стекового алгоритма, пример вычисления `2 3 4 * +` → 14.
- [ ] Написать раздел «Обработка ошибок» в отчёте.
- [ ] Нарисовать блок-схему функции `evaluate` (цикл по токенам, switch по типу).

**В отчёт пишет:** Раздел «Разработка → Вычислитель RPN», раздел «Разработка → Обработка ошибок» + блок-схема `evaluate`.

---

## Михалёв Кирилл Денисович — GUI

**Зона ответственности:** `src/main_window.{h,cpp}`, `src/main.cpp`.

- [ ] Прочитать `src/main_window.h` и `src/main_window.cpp`.
- [ ] Понять:
  - устройство Qt: QMainWindow, QWidget, layouts (QGridLayout, QVBoxLayout),
  - механизм сигналов и слотов (`connect(btn, SIGNAL(clicked()), this, SLOT(...))`),
  - как организованы кнопки (массив `buttons`, цикл создания),
  - как обрабатывается клавиатура (`keyPressEvent`).
- [ ] Сделать список всех элементов интерфейса (поле ввода, поле результата, кнопки 0-9, операции, скобки, =, C, ⌫, точка, ln/exp/sqrt/abs).
- [ ] Написать раздел «Графический интерфейс» в отчёте.
- [ ] Написать раздел «Используемые библиотеки» (Qt6 Widgets — указать версию, ссылку на документацию).
- [ ] Нарисовать схему компоновки окна (поле ввода сверху, результат, сетка 5×5 кнопок).

**В отчёт пишет:** Раздел «Проектирование → Используемые библиотеки», раздел «Проектирование → Описание компонентов GUI», раздел «Разработка → Связка GUI с парсером».

---

## Рыбин Тимофей Вячеславович — Тестирование

**Зона ответственности:** ручное и автоматическое тестирование, скриншоты.

- [ ] Установить себе Qt и собрать проект (помощь у Гаценко).
- [ ] Запустить `ctest --output-on-failure` и зафиксировать результат (41/41).
- [ ] Прогнать вручную все тест-кейсы из таблицы ниже, сделать **скриншоты** окна для каждого:
  - **Базовая арифметика:** `2+3*4` = 14, `(2+3)*4` = 20, `100/25` = 4
  - **Десятичные:** `3.14+0.86` = 4, `1.5*2` = 3
  - **Скобки:** `(1+(2*(3+4)))` = 15
  - **Функции:** `sqrt(16)` = 4, `abs(-7)` = 7, `ln(exp(1))` = 1, `exp(0)` = 1
  - **Композиции:** `2*sqrt(9)+abs(-3)` = 9, `(1+2)*sqrt(16)-abs(-5)` = 7
  - **Унарный минус:** `-5+10` = 5, `-(2+3)` = −5
  - **Ошибки (должно показать сообщение красным):** `1/0`, `ln(0)`, `sqrt(-4)`, `2++3`, `(2+3`, `foo(2)`
- [ ] Сложить скриншоты в `screenshots/` (папку создать).
- [ ] Написать раздел «Тестирование» в отчёте: таблица «Вход → Ожидаемый результат → Фактический результат → Скриншот».

**В отчёт пишет:** Раздел «Тестирование» (полностью, с таблицей и скриншотами).

---

## Шеянов Ярослав Владимирович — Отчёт и оформление

**Зона ответственности:** `report.md` (общий каркас), блок-схема общего алгоритма работы программы, оформление.

- [ ] Открыть `report.md`, ознакомиться со структурой.
- [ ] Написать раздел «Формулировка задания с указанием варианта» (взять из `task.md`, выделить наш вариант 2).
- [ ] Нарисовать общую блок-схему работы программы: ввод выражения → парсинг → вычисление → вывод результата ИЛИ ошибки. (Можно использовать draw.io или Lucidchart, экспорт в PNG, положить в `screenshots/`).
- [ ] Собрать в `report.md` куски от всех участников (Загорный, Калинин, Михалёв, Рыбин), привести к единому стилю.
- [ ] Проверить что отчёт содержит ВСЕ требуемые разделы из `task.md`:
  - Титульный лист (Гаценко)
  - Формулировка задания (ты)
  - Реализация: Проектирование (Михалёв), Разработка (Загорный + Калинин + Михалёв), Тестирование (Рыбин)
  - Заключение (Гаценко)
- [ ] Передать готовый `report.md` Гаценко на финальную проверку и экспорт в PDF/Word.

**В отчёт пишет:** Раздел «Формулировка задания», общая блок-схема, финальная сборка документа.

---

## Чек-лист сдачи

- [ ] Программа собирается на Mac (`cmake --build build -j` без ошибок).
- [ ] `ctest` показывает 41/41.
- [ ] GUI открывается, все кнопки работают, ошибки отображаются.
- [ ] Сделаны скриншоты по всем тест-кейсам.
- [ ] Отчёт содержит все разделы из `task.md`.
- [ ] Указан вариант 2 и состав команды.
- [ ] PDF/Word готов для сдачи Полтавскому Д. С. (каб. 440).

## Контакты

Все вопросы по коду / сборке — Гаценко Александр.
По отчёту — Шеянов Ярослав.
